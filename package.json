{
  "name": "subcontractor-platform",
  "private": true,
  "scripts": {
    "dev": "concurrently -n frontend,backend -c blue,green \"npm run dev --prefix frontend\" \"cd backend && uvicorn main:app --reload --port 8000\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
